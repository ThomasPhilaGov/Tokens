{
  "type": "module",
  "scripts": {
    "parse": "node parse-figma-css.js",
    "build:css": "npx sass colors.scss build/variables.css",
    "build": "npm run parse && npm run build:css"
  },
  "devDependencies": {
    "sass": "^1.85.0"
  }
}
